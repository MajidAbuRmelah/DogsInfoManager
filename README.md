# DogsInfoManager_Frontend

1. Clone the repository.
2. Open the terminal and execute:

```
npm install
```

That will install the required packages and dependencies. <br /><br />
3. Execute the code below in the terminal:

```
npm start
```
