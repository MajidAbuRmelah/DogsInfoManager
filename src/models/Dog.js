export default class Dog {
    /**
     * @param {number} id
     * @param {string} name
     * @param {number} age
     * @param {string} breed
     * @param {number} gender
     */
    constructor(id, name, age, breed, gender) {
        this.id = id;
        this.name = name;
        this.age = age;
        this.breed = breed;
        this.gender = gender;
    }
}