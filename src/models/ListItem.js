export default class ListItem {
    /**
     * @param {number} id
     * @param {string} title
     * @param {string} value
     */
    constructor(id, title, value) {
        this.id = id;
        this.title = title;
        this.value = value;
    }
}